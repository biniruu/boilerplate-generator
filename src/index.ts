const rootElem = document.getElementById('app') as HTMLDivElement
// TODO: remove the element below
const paramElem = document.createElement('p')
paramElem.textContent = 'Hello world'
rootElem.appendChild(paramElem)
